package service;

import models.Epic;
import models.SubTask;
import models.Task;

public class TaskManager {
    Epic epic = new Epic();
    Task task = new Task();
    SubTask subTask = new SubTask();
}
